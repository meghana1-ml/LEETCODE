class HelloWorld {
    static void Main() {
        int n = 1234;
        int rem =0;
        int res =0;
        do
        {
            rem=n%10;
            n=n/10;
            res=res * 10 + rem;
        }while(n!=0);
        
    Console.WriteLine(res);
        
    }
}
